<Window x:Class="Minesweeper.Game.Views.LeaderboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.Game.ViewModels"
        mc:Ignorable="d"
        Title="Зала на славата" Height="550" Width="550"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Window.DataContext>
        <local:LeaderboardViewModel />
    </Window.DataContext>

    <Border Background="White" CornerRadius="12" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.1" Color="Black" />
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                <TextBlock Text="🏆" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="ЗАЛАТА НА СЛАВАТА" FontSize="26" FontWeight="ExtraBold" Foreground="#1F2937" VerticalAlignment="Center"/>
            </StackPanel>

            <Separator Grid.Row="1" Margin="0,0,0,10" Background="#E5E7EB"/>

            <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">

                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Foreground" Value="#6B7280"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,3"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#3B82F6"/>
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#111827"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style TargetType="ListView">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Margin" Value="0,10,0,0"/>
                        <Setter Property="AlternationCount" Value="2"/>
                    </Style>

                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="#4B5563"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>

                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="5,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F3F4F6"/>
                        <Setter Property="Foreground" Value="#1F2937"/>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EFF6FF"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="Седмична">
                    <ListView ItemsSource="{Binding WeeklyScores}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ИГРАЧ" DisplayMemberBinding="{Binding User.Username}" Width="160"/>
                                <GridViewColumn Header="ВРЕМЕ" DisplayMemberBinding="{Binding TimeInSeconds, StringFormat={}{0} сек.}" Width="100"/>
                                <GridViewColumn Header="МИНИ" DisplayMemberBinding="{Binding MinesCount}" Width="90"/>
                                <GridViewColumn Header="ДАТА" DisplayMemberBinding="{Binding DatePlayed, StringFormat=dd.MM.yyyy}" Width="110"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>

                <TabItem Header="Месечна">
                    <ListView ItemsSource="{Binding MonthlyScores}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ИГРАЧ" DisplayMemberBinding="{Binding User.Username}" Width="160"/>
                                <GridViewColumn Header="ВРЕМЕ" DisplayMemberBinding="{Binding TimeInSeconds, StringFormat={}{0} сек.}" Width="100"/>
                                <GridViewColumn Header="МИНИ" DisplayMemberBinding="{Binding MinesCount}" Width="90"/>
                                <GridViewColumn Header="ДАТА" DisplayMemberBinding="{Binding DatePlayed, StringFormat=dd.MM.yyyy}" Width="110"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>

                <TabItem Header="All-Time">
                    <ListView ItemsSource="{Binding AllTimeScores}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ИГРАЧ" DisplayMemberBinding="{Binding User.Username}" Width="160"/>
                                <GridViewColumn Header="ВРЕМЕ" DisplayMemberBinding="{Binding TimeInSeconds, StringFormat={}{0} сек.}" Width="100"/>
                                <GridViewColumn Header="МИНИ" DisplayMemberBinding="{Binding MinesCount}" Width="90"/>
                                <GridViewColumn Header="ДАТА" DisplayMemberBinding="{Binding DatePlayed, StringFormat=dd.MM.yyyy}" Width="110"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </TabItem>

            </TabControl>
        </Grid>
    </Border>
</Window>