<Window x:Class="Minesweeper.Game.Views.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.Game.ViewModels"
        mc:Ignorable="d"
        Title="Minesweeper" Height="650" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="⏱️ " FontSize="20"/>
                <TextBlock Text="{Binding TimeElapsed}" FontSize="20" FontWeight="Bold" Foreground="#1F2937" VerticalAlignment="Center"/>
                <TextBlock Text=" сек." FontSize="16" Foreground="#4B5563" VerticalAlignment="Center" Margin="5,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="Рестарт (Нова игра)" Command="{Binding RestartCommand}" 
                        Padding="10,5" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,20,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock Text="Zoom: " VerticalAlignment="Center" Foreground="#4B5563"/>
                <Slider x:Name="ZoomSlider" Minimum="0.3" Maximum="3.0" Value="1.0" Width="100" VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="💣 Мини: " FontSize="16" Foreground="#4B5563" VerticalAlignment="Center"/>
                <TextBox Text="{Binding MineCount, UpdateSourceTrigger=PropertyChanged}" Width="40" TextAlignment="Center" FontSize="16" FontWeight="Bold" Padding="2" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding Board}" HorizontalAlignment="Center" VerticalAlignment="Center">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="15" Columns="15"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Content}" 
                                Background="{Binding Background}"
                                Foreground="{Binding Foreground}"
                                FontWeight="Bold" FontSize="16"
                                Width="30" Height="30" Margin="1"
                                Command="{Binding DataContext.RevealCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                CommandParameter="{Binding}">

                            <Button.InputBindings>
                                <MouseBinding MouseAction="RightClick" 
                                              Command="{Binding DataContext.FlagCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                              CommandParameter="{Binding}"/>
                            </Button.InputBindings>

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="Gray" BorderThickness="1" CornerRadius="2">
                                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.LayoutTransform>
                    <ScaleTransform ScaleX="{Binding ElementName=ZoomSlider, Path=Value}" 
                                    ScaleY="{Binding ElementName=ZoomSlider, Path=Value}"/>
                </ItemsControl.LayoutTransform>
            </ItemsControl>

        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="{Binding GameStatus}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#DC2626"/>
    </Grid>
</Window>